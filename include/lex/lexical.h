#ifndef LEX_LEXICAL_H
#define LEX_LEXICAL_H

#include<fstream>
#include<string>
#include"lex/token.h"
#include"exceptions.h"

using namespace std;


class Lex {
public:
    Lex(string fileName) throw(IOException);

    ~Lex();

    Token getNextToken(void) throw(Exception);

private:
    TokenType getType(const string &s);    // ??????????????????????id
    bool slipping();                  // ????????????????????????false
    string getWord();                 // ??cur???????????????????
    Token getNum();                   // ??????????
    Token getStr();                   // ????????
    Token getSym();                   // ?????????
private:
    ifstream fin;              // ???????
    char cur;                  // ?????????????????δ???
    int line;                  // ????????????
    int offset;                // ???????????????
};

#endif // !LEX_LEXICAL_H
